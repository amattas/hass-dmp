# Copy to: website/.npmrc
# min-release-age requires npm >= 11.10 (bundled with current Node 24): newly published
# package versions are ignored for 7 days — cheap supply-chain protection.
engine-strict=true
ignore-scripts=true
min-release-age=7
